@define-mixin header-link {
    display: block;
    text-decoration: none;
    text-transform: uppercase;
    font-size: 18px;
    font-weight: 600;
    color: $color-white;

    @mixin responsive-le-l {
        font-size: 16px;
    }

    @mixin responsive-le-m {
        font-size: 14px;
    }

    @mixin responsive-le-xs {
        font-size: 12px;
    }

    a&:hover,
    a&.is-active {
        color: $color-green !important;
        text-decoration: underline;
    }
}

.header {
    position: absolute;
    width: 100%;
    min-height: 100px;
    left: 0px;
    top: 0px;
    box-sizing: border-box;
    gap: 10px;
    z-index: 30;
    &:before {
        content: '';
        position: absolute;
        width: 100%;
        height: 100px;
        top: 0;
        left: 0;
        background: linear-gradient(111.84deg, rgba(255, 255, 255, 1) 0%, rgba(255, 255, 255, 0.2) 90%);
        border: 1.2px solid rgba(255, 255, 255, 0.5);
        box-shadow: 5px 6px 20px rgba(0, 0, 0, 0.15);
        backdrop-filter: blur(2px);
        opacity: .6;
        z-index: -1;
    }
    &.m--seconds {
        position: relative;
        top: auto;
        left: auto;
        background-image: url('./assets/img/bg/header_seconds.png');
    }
    &.m--cabinet {
        position: relative;
        top: auto;
        left: auto;
        background: none;
    }
    &__content {
        position: relative;
        display: flex;
        justify-content: space-between;
        padding: 15px 0;
        height: 100px;

        @mixin responsive-le-s {
            flex-direction: column-reverse;
        }
    }

    &__left,
    &__right {
        position: relative;
        display: flex;
        align-items: center;
        @mixin responsive-le-l {
            gap: 12px;
        }
    }

    &__left {
        gap: 48px;

        @mixin responsive-le-xl {
            gap: 24px;
        }

        @mixin responsive-le-s {
            flex-direction: column;
            gap: 0;
            height: 100%;
            justify-content: space-around;
        }
    }

    &__right {
        gap: 48px;

        @mixin responsive-le-xl {
            gap: 24px;
        }

        @mixin responsive-le-s {
            position: absolute;
            top: 15px;
            right: 0;
            gap: 0;
        }

        @mixin responsive-le-xs {
            top: 18px;
        }
    }

    &.m--cabinet &__burger {
        display: none;
        width: 54px;
        height: 54px;
        background-color: $color-red;
        -webkit-mask-position: center;
        mask-position: center;
        -webkit-mask-repeat: no-repeat;
        mask-repeat: no-repeat;
        -webkit-mask-size: contain;
        mask-size: contain;
        -webkit-mask-image: url('./assets/img/icons/icon-burger.svg');
        mask-image: url('./assets/img/icons/icon-burger.svg');

        &:hover,
        &.is-active {
            background-color: $color-green;
        }

        @mixin responsive-le-m {
            display: block;
        }

        @mixin responsive-le-s {
            position: absolute;
            top: 0;
            left: 0;
            width: 45px;
            height: 45px;
        }

        @mixin responsive-le-xs {
            top: 3px;
            width: 38px;
            height: 38px;
        }
    }

    &__logo {
        width: 240px;

        @mixin responsive-le-l {
            width: 200px;
        }

        @mixin responsive-le-m {
            width: 180px;
        }

        @mixin responsive-le-s {
            margin-right: auto;
        }

        @mixin responsive-le-xs {
            width: 154px;
        }

        &-link,
        &-img {
            width: 100%;
        }
    }

    &.m--cabinet &__logo {
        @mixin responsive-le-s {
            margin-left: auto;
        }
    }

    &__contacts {
        @mixin responsive-le-m {
            display: none;
        }

        &-phone {
            @mixin header-link;
            margin-bottom: 8px;
        }

        &-email {
            @mixin header-link;
        }
    }

    &.m--cabinet &__contacts {
        &-phone {
            color: $color-black;
        }

        &-email {
            color: $color-black;
        }
    }

    &__timer {
        text-align: right;

        @mixin responsive-le-m {
            display: none;
        }

        &-time,
        &-date {
            position: relative;
            display: block;
            padding-right: 32px;

            &:before {
                content: '';
                position: absolute;
                top: 50%;
                right: 0;
                margin-top: -10px;
                width: 20px;
                height: 20px;
            }
        }

        &-time {
            @mixin header-link;
            margin-bottom: 8px;
            &:before {
                background: url('./assets/img/icons/icon-timer-time.svg') 50% 50% no-repeat;
                background-size: 100% 100%;
            }
        }

        &-date {
            @mixin header-link;
            &:before {
                background: url('./assets/img/icons/icon-timer-date.svg') 50% 50% no-repeat;
                background-size: 100% 100%;
            }
        }
    }

    &.m--cabinet &__timer {
        &-time {
            color: $color-black;
        }

        &-date {
            color: $color-black;
        }
    }

    &__menu {
        display: flex;
        gap: 24px;
        padding: 0;
        margin: 0 12px 0 0;
        list-style: none;

        @mixin responsive-le-l {
            gap: 12px;
        }

        @mixin responsive-le-s {
            width: 100%;
            justify-content: left;
        }

        &-item {
            margin: 0;
            display: flex;
            align-items: center;
            
            @mixin responsive-le-s {
                margin: 0;
            }

            @mixin responsive-le-xs {
                text-align: center;
            }
        }

        &-link {
            @mixin header-link;
        }
    }

    &.m--cabinet &__menu {
        &-link {
            color: $color-black;
        }

        @mixin responsive-le-s {
            width: 100%;
            justify-content: center;
        }
    }

    &__login {
        width: 150px;
        height: 60px;

        @mixin responsive-le-l {
            width: 110px;
            height: 40px;
            font-size: 16px;
        }

        @mixin responsive-le-m {
            width: 90px;
            font-size: 14px;
        }
    }

    &__user {
        width: 54px;
        height: 54px;
        background-color: $color-red;
        -webkit-mask-position: center;
        mask-position: center;
        -webkit-mask-repeat: no-repeat;
        mask-repeat: no-repeat;
        -webkit-mask-size: contain;
        mask-size: contain;
        -webkit-mask-image: url('./assets/img/icons/icon-user.svg');
        mask-image: url('./assets/img/icons/icon-user.svg');

        &:hover,
        &.is-active {
            background-color: $color-green;
        }

        @mixin responsive-le-s {
            width: 45px;
            height: 45px;
        }

        @mixin responsive-le-xs {
            width: 38px;
            height: 38px;
        }
    }

    &__sub {
        padding: 40px 0;
    }

    &__breadcrumbs {
        font-weight: 300;
        font-size: 12px;
        line-height: 15px;
        margin-bottom: 10px;

        &-link {
            position: relative;
            display: inline-block;
            color: $color-white;
            margin-bottom: 8px;

            &:after {
                content: '—';
                position: relative;
                display: inline-block;
                margin: 0 10px;
            }
        }
    }

    &__title {
        color: $color-white;
        margin-top: 0;
    }

    &__popup {
        position: absolute;
        top: 100%;
        right: 0;
        margin-top: 30px;
        padding: 30px;
        background: $color-bg;
        box-shadow: 0px 16px 24px rgba(0, 0, 0, 0.06), 0px 2px 6px rgba(0, 0, 0, 0.04), 0px 0px 1px rgba(0, 0, 0, 0.04);
        border-radius: 28px;
        width: 100%;
        max-width: 450px;

        @mixin responsive-le-m {
            width: 450px;
        }

        @mixin responsive-le-xs {
            width: 90vw;
        }

        &-title {
            position: relative;
            color: $color-gray-T1;
            font-weight: 700;
            font-size: 24px;
            line-height: 28px;
            text-transform: uppercase;
            padding: 0 40px 24px 3px;
            border-bottom: 1px solid $color-gray;
        }

        &-close {
            position: absolute;
            top: 0;
            right: 0;
            width: 28px;
            height: 28px;
            border: 2px solid $color-green;
            border-radius: 2px;

            &:before {
                content: '';
                position: absolute;
                top: 50%;
                left: 50%;
                margin-top: -6px;
                margin-left: -6px;
                width: 12px;
                height: 12px;
                background-color: $color-green;
                -webkit-mask-position: center;
                mask-position: center;
                -webkit-mask-repeat: no-repeat;
                mask-repeat: no-repeat;
                -webkit-mask-size: contain;
                mask-size: contain;
                -webkit-mask-image: url('./assets/img/icons/icon-close.svg');
                mask-image: url('./assets/img/icons/icon-close.svg');
            }

            &:hover {
                border: 2px solid $color-red;

                &:before {
                    background-color: $color-red;
                }
            }
        }

        &-body {
            padding: 24px 3px 0;
        }

        &-footer {
            padding: 24px 3px 0;
            border-top: 1px solid $color-gray;

            .button {
                width: 260px;
                padding: 12px 16px;
                line-height: 22px;
                margin: 0 auto;
            }
        }
        &-menu {
            display: flex;
            flex-direction: column;
            align-items: flex-start;

            &-item {
                font-weight: 700;
                font-size: 18px;
                line-height: 22px;
                text-transform: uppercase;
                margin-bottom: 24px;
            }

            &-link {
                color: $color-gray-T1;
                text-decoration: none;

                &:hover,
                &.is-active {
                    color: $color-green;
                }
            }
        }
    }
}
/*
.m--page404.m--with-sidebar .header:after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 100px;
    background-color: $color-bg;
    z-index: -1;
}
*/
